import React from 'react'
import ReactDOM from 'react-dom'
import ContentManagementTool from './ContentManagementTool'

ReactDOM.render(
  <React.StrictMode>
    <ContentManagementTool />
  </React.StrictMode>,
  document.getElementById('root'),
)
